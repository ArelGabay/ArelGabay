- name: Set up Python
  uses: actions/setup-python@v4
  with:
    python-version: '3.11'  # Or any version you want

- name: Install Python dependencies
  run: |
    python -m pip install --upgrade pip
    pip install -r requirements.txt
